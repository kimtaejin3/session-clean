# sclean

여러 프로젝트에 쌓인 Claude Code 세션을 한 화면에서 확인하고, 설명 가능한 규칙으로 추천받아, 한 번에 안전하게 정리하는 로컬 터미널 UI.

- 네트워크를 쓰지 않습니다. 계정도, 텔레메트리도 없습니다.
- 시작할 때 아무것도 선택되어 있지 않습니다. 지우는 것은 언제나 사용자가 결정합니다.
- 모든 정리는 트랜잭션입니다. 중간에 실패하면 옮긴 파일을 전부 되돌립니다.
- 휴지통과 복원이 있습니다. 자동으로 비워지지 않습니다.

전체 요구사항은 [docs/PRD.md](docs/PRD.md), 구현 계획은 [docs/superpowers/plans/2026-08-26-sclean-v0.1.md](docs/superpowers/plans/2026-08-26-sclean-v0.1.md)에 있습니다.

## 설치

```sh
cargo install --path .
```

Rust 1.85 이상(edition 2024)이 필요합니다. 1차 검증 환경은 Apple Silicon macOS입니다.

## 실행

```sh
sclean
```

옵션은 없습니다. 세션 선택, 기준 변경, 휴지통 이동, 완전 삭제, 복원은 모두 TUI 안에서 합니다.

## 조작

왼쪽은 프로젝트 목록, 오른쪽은 **고른 프로젝트의 세션**입니다. `→`로 세션 목록에 들어가고 `←`로 돌아옵니다.
터미널이 좁으면 두 패널 중 지금 있는 쪽만 전체 폭으로 보여줍니다.

| 키 | 동작 |
|---|---|
| `↑` `↓` | 항목 이동 (지금 있는 패널 안에서) |
| `→` | 고른 프로젝트의 세션 목록으로 |
| `←` | 프로젝트 목록으로 돌아가기 |
| `Space` | 선택·해제 (프로젝트 목록에서는 그 프로젝트 전체) |
| `A` | 현재 검색에 걸린 추천 항목 전체 선택·해제 |
| `/` | 프로젝트·세션 검색 |
| `D` | 선택 항목 정리 |
| `T` | 휴지통 화면 |
| `F` | 추천 기준 화면 |
| `?` | 도움말 |
| `Q` | 종료 |

정리 확인 화면에서는 `←` `→`로 `휴지통 이동`과 `완전 삭제`를 고릅니다. 완전 삭제는 `DELETE`를 직접 입력해야 실행됩니다.

휴지통 화면에서는 `R` 복원, `X` 영구 삭제, `Enter` 상세 보기입니다.

## 추천 규칙

하나라도 맞으면 추천으로 표시하고, 왜 추천하는지 한 줄로 보여줍니다. 규칙은 `F` 화면에서 각각 켜고 끌 수 있습니다.

| 규칙 | 조건 | 기본값 |
|---|---|---|
| R1 오래된 세션 | 마지막 활동이 기준일보다 오래됨 | 켜짐 / 30일 |
| R2 존재하지 않는 프로젝트 | 세션의 `cwd`가 지금 파일시스템에 없음 | 켜짐 |
| R3 짧은 세션 | 사용자 메시지 1개 이하 + 도구 실행 없음 | 켜짐 |
| R4 종료된 하위 에이전트 | 하위 에이전트 세션이고 최근 활동이 없음 | 켜짐 |
| R5 고아 데이터 | 대화 기록 없이 연결 데이터만 남음 | 켜짐 |

다음 중 하나라도 해당하면 **추천하지 않거나 정리를 차단**합니다.

- 세션 형식을 분석할 수 없음 (차단)
- 연결 데이터의 소유 세션을 확정할 수 없음 (차단)
- 지금 실행 중인 세션 (차단)
- 스캔 이후 파일의 크기나 수정 시각이 바뀜 (차단)
- 프로젝트 경로를 신뢰할 수 있게 확인하지 못함 (추천 보류)
- 5분 안에 활동한 세션 (추천 보류)

## 다루는 데이터

읽는 곳 — 실제로 존재하는 경로만 읽고, 없는 경로는 오류로 보지 않습니다.

```text
~/.claude/projects/<프로젝트>/<세션>.jsonl   대화 기록
~/.claude/projects/<프로젝트>/<세션>/        하위 에이전트 기록
~/.claude/tasks/  teams/                     세션 ID 앞 8자로 연결 (유일할 때만 소유로 인정)
~/.claude/session-env/  file-history/  todos/  debug/
~/.claude/sessions/                          실행 중 세션 잠금 (읽기만)
~/.claude/history.jsonl                      공유 기록 (소유가 확정되는 줄만 수정)
```

쓰는 곳 — 여기에만 씁니다.

```text
~/Library/Application Support/sclean/
├── config.json      추천 기준
├── sclean.log       로컬 로그 (프롬프트 본문은 기록하지 않습니다)
└── trash/<작업ID>/  manifest.json + 옮겨둔 파일
```

프로젝트 소스 파일은 **존재 확인 외에 읽지도 쓰지도 않습니다.**

## 안전 장치

- 실행 직전에 모든 대상 파일을 다시 확인하고, 스캔 이후 바뀐 세션은 제외합니다.
- 모든 대상 경로가 `~/.claude` 안에 있는지 검증합니다. 하나라도 벗어나면 작업 전체를 실행하지 않습니다. 심볼릭 링크는 따라가지 않습니다.
- 파일을 옮기기 **전에** `manifest.json`을 먼저 기록합니다. 도중에 강제 종료되어도 다음 실행에서 복구를 제안합니다.
- `history.jsonl`처럼 공유되는 파일은 백업 후 임시 파일에 쓰고 원자적으로 교체합니다.
- 복원할 때 원래 경로에 파일이 있으면 덮어쓰지 않고 충돌로 표시합니다.
- 삭제와 복원은 반복 실행해도 안전합니다.

## 개발

```sh
cargo test                       # 158개 테스트
cargo clippy --all-targets -- -D warnings
cargo fmt --check
cargo test --release --test perf_test -- --nocapture   # 2,000 세션 스캔 시간
```

테스트는 실제 `~/.claude`를 절대 건드리지 않고 임시 디렉터리 fixture만 씁니다.
수동 확인이 필요할 때는 데이터 위치를 바꿔 실행할 수 있습니다.

```sh
SCLEAN_CLAUDE_DIR=/tmp/fake-claude SCLEAN_DATA_DIR=/tmp/fake-sclean sclean
```

## 아직 하지 않는 것 (v0.1)

세션 실행·재개, 실시간 모니터링, AI 기반 분류, 백그라운드 자동 정리, 클라우드 동기화,
네이티브 GUI, Codex·OpenCode 지원, Homebrew 배포, 공유 paste cache·전역 plan·telemetry 정리.
